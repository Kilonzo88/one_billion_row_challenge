[package]
name = "one_billion_row_challenge"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = "fat"
panic = "abort"
debug = true
codegen-units = 1


[dependencies]
rand = "0.8"
rand_distr = "0.4"
